services:
  app:
    image: myapp:latest
    environment:
      DB_HOST: db
      DB_PASSWORD: secret123
    depends_on:
      - db
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: secret123
